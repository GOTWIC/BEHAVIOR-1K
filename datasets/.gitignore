# Ignore everything
*

# Re-include the .gitignore file itself
!.gitignore
!.dockerignore

# Also include the README
!README

# Track custom_dataset (user/local assets, e.g. custom robots)
!custom_dataset/
!custom_dataset/**